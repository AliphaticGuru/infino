# Laravel docker factory
## how to use
configure dockerfile as needed by installing or removing extensions as needed.
Build image using "docker build -t yourusername/imagename:tag"

configure docker compose as needed and also feel free to use the nginx config for your nginx setup

#### All the best